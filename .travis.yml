os: linux
dist: bionic

language: node_js
node_js: 12
cache: npm

script:
  - npm run lint
  - npm run test

deploy:
  provider: npm
  email: $NPM_EMAIL_ADDRESS
  api_token: $NPM_AUTH_TOKEN
  on:
    tags: true
